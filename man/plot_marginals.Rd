% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{plot_marginals}
\alias{plot_marginals}
\title{Marginal plots for \code{ls_optim} fits (base graphics)}
\usage{
plot_marginals(
  object,
  variables = NULL,
  point_alpha = 0.6,
  point_cex = 0.8,
  line_lwd = 2,
  npoints_line = 200
)
}
\arguments{
\item{object}{An \code{"ls_optim"} fit returned by \code{\link{my_lm}}.}

\item{variables}{Optional subset of variables to plot, as names or integer indices.
Defaults to all non-intercept columns.}

\item{point_alpha}{Point opacity (0–1) passed to \code{\link[grDevices]{rgb}} for plotting.}

\item{point_cex}{Point size (passed to \code{\link[graphics]{plot}}).}

\item{line_lwd}{Line width for the marginal fit lines.}

\item{npoints_line}{Number of points for the line grid along each predictor.}
}
\value{
\code{NULL} (invisibly). Called for its side effect of plotting.
}
\description{
Produces scatter plots of \code{y} versus each predictor and overlays a
“marginal” fitted line for variable \eqn{x_j} defined as
\eqn{\alpha + \beta_j x_j + \sum_{k \neq j} \beta_k \bar X_k}, i.e., holding
other covariates at their sample means.
}
\examples{
\donttest{
set.seed(1)
n <- 80; p <- 2
X <- matrix(rnorm(n*p), n, p); colnames(X) <- c("x1","x2")
y <- as.vector(cbind(1, X) \%*\% c(1, 2, -1) + rnorm(n))
fit <- my_lm(X, y, B = 30)
plot_marginals(fit)
}

}
